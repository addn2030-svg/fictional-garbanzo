# P0 — Production Truth Test (اختبار الحقيقة الإنتاجي)

> **القاعدة:** لا تُضاف أي feature قبل إغلاق كل FAIL. النتيجة تُسجَّل من **Telegram الفعلي على المنشور الإنتاجي** (وليس من اختبارات الوحدة ولا من جهاز التطوير). كل سطر ينتهي بـ PASS أو FAIL مع دليل (لقطة/نص الرد/وقت).
>
> المالك: Abdulrahman · التاريخ المستهدف: قبل 13 سبتمبر 2026 · الحالة العامة: **🟡 قيد التنفيذ — T00 ✅ (2026-09-06) والمتبقي T01–T08**

> ملاحظة محسومة: فشل Voice/Transcribe في `/selftest` **اختياري غير حاجز** — لا يدخل في T01–T08، آمن عند الإطفاء، ويُعالج لاحقًا إن رُغب في إدخال الصوت (يتطلب اعتماد AWS S3/Transcribe).

## بيئة الاختبار

- المنشور الإنتاجي: ____________ (Railway / autostart) — حدّد الرابط/الخدمة.
- حساب Telegram المختبر: ____________ (يجب أن يكون ضمن chat IDs المسموحة).
- إصدار الكود قيد الاختبار: commit SHA ____________ على `main`.
- ابدأ من حالة نظيمة: سجّل `/storage_status` و`/selftest` أولًا واحفظ ناتجهما.

## حالات الاختبار

| # | المسار | الخطوة من Telegram | معيار النجاح (PASS) | النتيجة | الدليل |
|---|---|---|---|---|---|
| T00 | البوت حي + فحص ذاتي | أرسل `/selftest` | كل مكونات P0 الأساسية مهيأة؛ بوابة `TELEGRAM_ALLOWED_CHAT_ID` مفعّلة | 🟢 PASS | 2026-09-06: 9/10 — Telegram/Manager/ChiefOfStaff/Store/Knowledge/Skills/Bedrock/Sheets/Memory كلها ✅. ❌ الوحيد Voice/Transcribe (اختياري، غير حاجز — يحتاج AWS_S3_AUDIO_BUCKET + AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY). الحماية: allowlist مفعّلة |
| T01 | البريف | أرسل `/brief` | رد خلال مهلة معقولة؛ قسم إشارات/قيود ظاهر (Executive Brief v3)؛ لا أخطاء؛ لا ادعاء "لا أستطيع" كاذب | ⏳ | |
| T02 | قراءة الشيت | اطلب قراءة خلية/نطاق من Main Sheet | قيمة فعلية صحيحة من الشيت؛ Capability Truth يؤكد الوصول (لا إنكار كاذب) | ⏳ | |
| T03 | تنفيذ إجراء | اطلب إجراءً (تذكير/موعد) → يظهر preview → `/approve_action <id>` بالبصمة الصحيحة → تنفيذ → **إيصال receipt** | السلسلة كاملة: preview ثم رفض الموافقة الخاطئة للبصمة ثم تنفيذ بعد الموافقة الصحيحة ثم إيصال يحمل معرّف الإجراء | ⏳ | |
| T03b | رفض إجراء بلا موافقة | نفس الإجراء بدون `/approve_action` | يبقى PENDING_APPROVAL ولا ينفّذ شيئًا؛ تنتهي صلاحيته بعد 48 ساعة | ⏳ | |
| T04 | التقويم | اسأل عن أحداث اليوم/الغد + أنشئ تذكيرًا | حدث حقيقي يظهر؛ التذكير يُجدول؛ الطلب الغامض يُرفض بإغلاق آمن (fail-closed) لا بتخمين | ⏳ | |
| T04b | Commerce limits | جرّب `/shop` لطلب > 375 ريال، وطلب ضمن الحد | >375 يُرفض صراحةً بالسقف؛ ضمن الحد يتطلب `/approve_order` ثم إيصال؛ سقف اليوم 375 يوقف طلبًا ثانيًا يتجاوزه | ⏳ | |
| T05 | memory readback | أرسل `/books` واستعلام كتب | قائمة كتب من الشيت عبر fast path؛ **لا** استدعاء LLM (تحقق من السجلات/زمن الاستجابة) | ⏳ | |
| T05b | الذاكرة التشغيلية | اطلب `/tasks` ثم `/decisions` | تطابق ما في StateStore؛ عناصر waiting_for الظاهرة صحيحة | ⏳ | |
| T06 | **الثبات بعد إعادة النشر** | سجّل لقطة حالة (مهمة/قرار/إجراء) → نفّذ restart/redeploy للمنشور → أعد `/brief` و`/tasks` | كل العناصر ما زالت موجودة بعد إعادة التشغيل (StateStore persistence)؛ لا فقدان حالة | ⏳ | |
| T07 | **سلامة النقل 24 ساعة** | بعد إعادة النشر، راقب سجلات الـ webhook/polling لمدة 24 ساعة | **صفر خطأ Telegram 409** (Conflict: terminated by other getUpdates) طوال 24 ساعة؛ لا تكرار رسائل؛ لا انقطاع دورة المدير | ⏳ | |
| T08 | /selftest بعد الاستقرار | أرسل `/selftest` بعد T07 | كل المكونات الأساسية خضراء | ⏳ | |

## قواعد التسجيل

1. كل FAIL يُسجّل في جدول "الإخفاقات المفتوحة" أدناه، ويوقف أي عمل على ميزات جديدة حتى يُغلق (القرار D1).
2. النتيجة تُحدَّث فورًا: `⏳` لم يُبدأ · `🟢 PASS` · `🔴 FAIL` · `🚫 BLOCKED` (مع السبب).
3. لا يُقبل PASS إلا بدليل (نص الرد أو لقطة أو سطر سجل).

## الإخفاقات المفتوحة (لا تُضف ميزة قبل إغلاقها)

| # | الاختبار | وصف الفشل | المسار/الإصلاح | الحالة | أُغلق في |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

## شرط النجاح الكامل لـ P0

- T01–T06 وT08 كلها 🟢.
- T07 🟢 بعد مرور 24 ساعة كاملة على المنشور (هذا الاختبار يمتد زمنيًا — يُفتح اليوم ويُغلق غدًا).
- عندها فقط يُرفع تجميد D1 جزئيًا ويُنتقل إلى نشر ذاكرة Drive (P1) وفق بوابتها.

# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# data (runtime state)
/data/

# misc
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
*.tsbuildinfo
/posters/

# Created via the GitHub web UI by the owner (Arena's app can't push
# workflow files without the 'workflows' permission).
.github/workflows/publish.yml
